# Copy this file to `.env` and fill in your real values.
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Two-tier model routing. Each pipeline stage is tagged as "primary"
# (structural / voice-critical) or "cheap" (bulk descriptive content).
# Override per-stage in a seed / brief via `stage_models:`, or pin all
# stages globally with `model:` in the seed / brief or `--model` on the
# CLI.
PRIMARY_MODEL=anthropic/claude-sonnet-4.6
CHEAP_MODEL=deepseek/deepseek-v3.2

# Legacy single-model default. Used as a fallback for the primary tier
# only if PRIMARY_MODEL is unset; kept for backwards compatibility.
CAMPAIGN_GENERATOR_DEFAULT_MODEL=anthropic/claude-sonnet-4.5
CAMPAIGN_GENERATOR_DRY_RUN_MODEL=anthropic/claude-haiku-4.5
CAMPAIGN_GENERATOR_DEFAULT_TEMPERATURE=0.8

# Optional base directories.
# With CAMPAIGN_GENERATOR_GENRES_BASE_DIR set, you can pass `--genre symbaroum_dark_fantasy`
# instead of a full path.
CAMPAIGN_GENERATOR_GENRES_BASE_DIR=../genres
#
# With CAMPAIGN_GENERATOR_CAMPAIGNS_BASE_DIR set, `--output` becomes optional and the
# generator will create a folder automatically, for example:
# 20260421_153000_symbaroum_dark_fantasy_my_seed
CAMPAIGN_GENERATOR_CAMPAIGNS_BASE_DIR=./campaigns

# Optional transport/retry knobs.
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_TIMEOUT_SECONDS=120
OPENROUTER_MAX_RETRIES=3
CAMPAIGN_GENERATOR_STAGE_MAX_RETRIES=3

# NPC portrait generation (used by `python -m image_generator`).
# IMAGE_GEN_MODEL is required when running the image tool — no fallback is provided.
IMAGE_GEN_MODEL=google/gemini-3-pro-image-preview
# Long-edge size in pixels; the short edge is derived from IMAGE_GEN_ASPECT_RATIO.
IMAGE_GEN_DIMENSION=1024
# Aspect ratio as W:H. Portrait NPCs typically look best at 9:16.
IMAGE_GEN_ASPECT_RATIO=9:16
# Optional: force a single visual direction when re-rendering an existing campaign.
IMAGE_GEN_STYLE_OVERRIDE=Full-body photorealistic character portrait, realistic skin texture, natural anatomy, cinematic lighting. No illustration, comic, painting, or sketch look.
