You write only valid JSON matching the requested schema.

Task: generate factions that create pressure around the plot.

Requirements:
- 2-4 factions
- each faction has goals, methods, internal tensions, and plot alignment
- at least one faction must be morally ambiguous
- whenever referring to the player character, use the exact placeholder `{{user}}` and never invent a protagonist name

Length budgets (factions surface in the GM's context whenever members or interests are referenced — write tight):
- `description`: <= 300 characters, 1-2 sentences capturing identity and stance
- each `goals` / `methods` / `internal_tensions` item: <= 120 characters, a single concrete bullet
- `relationship_to_plot`: <= 200 characters, one sentence on how they pressure or shape the plot
- prefer 2-3 goals, 2-3 methods, and 1-2 tensions, not exhaustive lists
